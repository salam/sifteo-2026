Add Cube Manager GUI. Testing different siftapp install flows (wip).

Code:
- add `python3 -m sifteo gui` with pywebview frontend/backend bridge, bundled game catalog, install progress, and upload tuning controls (`src/sifteo/gui/*`)
- add CLI command `install-siftapp` and expose new package APIs for `.siftapp` helpers/results (`src/sifteo/__main__.py`, `src/sifteo/__init__.py`, `src/sifteo/assets.py`)
- implement legacy-style per-write dongle ACK handling plus ACK timeout tuning (`SIFTEO_WRITE_ACK_TIMEOUT`) and queue-safe RX overflow behavior (`src/sifteo/dongle.py`)
- make asset upload drain stale dongle responses before transfer and require ACK-synchronized header/chunk writes (`src/sifteo/assets.py`)
- fix upload chunk sizing to fit transport payload budget (`USB_MSG_LEN - 4` envelope), preventing silent full-chunk truncation and cube-side misalignment errors (`src/sifteo/assets.py`)
- align upload-result status decode priority with legacy offset (`payload[6]`) and retain fallback offsets for firmware variants (`src/sifteo/assets.py`)
- auto-add trailing CRC32 for `.siftapp` payload uploads when missing so cube-side asset CRC validation can pass (`src/sifteo/assets.py`)
- run `.siftapp` installs through direct dongle + `AssetManager` flow (without full runner cube initialization) to reduce radio/control noise during installs (`src/sifteo/__main__.py`)
- default `.siftapp` app-id inference to `crc32(filename_stem)` and keep header-derived IDs as opt-in (`--prefer-header-app-id`) to avoid bundle ID collisions (`src/sifteo/assets.py`, `src/sifteo/__main__.py`)
- add optional `--asset-type` CLI override, auto compatibility retries (`crc/header` app-id x type 0/1), and richer upload-status diagnostics (`src/sifteo/__main__.py`, `src/sifteo/assets.py`)
- update helper scripts for bundled app installs and env passthrough of upload tuning variables (`examples/install_app.py`, `examples/asset_upload.py`)
- add optional GUI dependency metadata (`pyproject.toml`)

Docs:
- expand README with GUI quick start, `.siftapp` install usage, and upload tuning details (`README.md`)
- update TODO progress and unreleased changelog entries to match current implementation (`TODO.md`, `CHANGELOG.md`)

Tests:
- add `.siftapp` parsing/install tests including filename-based default app-id inference and header-id opt-in behavior (`tests/test_siftapp.py`)
- add `.siftapp` CRC-wrapper tests to verify trailing CRC handling (`tests/test_siftapp.py`)
- add upload-chunk payload budget test to prevent future chunk truncation regressions (`tests/test_siftapp.py`)
- add dongle queue overflow, env tuning, and ACK handshake tests (`tests/test_dongle.py`)

Assets/Artifacts:
- add Cube Manager screenshot (`Screenshot Sifteo-GUI.png`)

Tests:
- `pytest -q tests/test_dongle.py tests/test_siftapp.py` (passed: 20 passed)
- `PYTHONPATH=src python3 -m sifteo install-siftapp --help` (passed)
- `pytest -q` (fails during collection on legacy/extracted Python 2 trees and missing optional deps such as `usb`)
