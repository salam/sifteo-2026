Add Cube Manager GUI. Testing different siftapp install flows (wip).

GUI:
- Add pywebview-based Cube Manager GUI (`python3 -m sifteo gui`) with dark-themed
  single-page app: landing screen (dongle detection, sudo escalation) and connected
  dashboard (game card grid with cover art, inline install progress)
- Add Python<->JS bridge with status polling, connect/disconnect, game catalog,
  and install-with-progress
- Add hand-curated metadata for all 23 bundled legacy games (display names,
  categories, descriptions) with cover art extracted from original Sifteo Sync app
- Add settings panel with upload speed slider (log-scale, 25 ms to 1 µs write gap)
  and write retries control, applied live to dongle
- Add category filter dropdown and sort buttons (A-Z, Size) in game dashboard toolbar
- Add toggleable console log panel with timestamped status/connection/install/error
  messages from both JS frontend and Python bridge
- Set custom macOS dock icon (SifteoSync.icns) via AppKit at launch
- Add Observer tab in dashboard for real-time cube visualization:
  cubes rendered as rounded squares with directional arrow, tilt-based 3D rotation
  (perspective transform), button press green circle indicator, battery low warning,
  shake pulse animation (3 concentric rings), BFS neighbor-aware layout engine, and
  docking station grid (2x3 slot layout)
- Add neighbor debug logging to console panel in Observer tab

Protocol & cube:
- Add NEIGHBOR_FULL_REPORT (op 137) handler in cube.py so neighbor state is
  populated on initial cube connection, not only on live add/remove events
- Confirm authoritative side numbering 0=top, 1=left, 2=bottom, 3=right from
  decompiled Sifteo SDK (Cube.Side enum, SIDE_NAMES)
- Add CRC-learning upload (upload_bytes_learn_crc): two-pass strategy that probes
  the cube's proprietary CRC, then re-uploads with the correct CRC appended
- Add UploadResult dataclass with per-upload status and firmware-reported CRC values
- Refactor asset upload internals: upload_bytes() delegates to _upload_raw()
  returning rich UploadResult, enabling CRC-learning strategy
- Add bundle extraction pipeline: AppPackager-compatible decrypt, ZIP unpack,
  .sftbndl/index parsing, and per-image asset install support
- Add install-siftapp auto-try compatibility variants (crc/header app-id, asset-type)
- Add install-siftapp payload shape auto-detection (container vs body bytes)
- Add install-siftapp install-mode auto (bundle first, opaque fallback)
- Asset uploads now perform legacy-style per-packet dongle ACK synchronization
- Dongle write path now rate-limits and retries transient USB timeout writes
- Dongle write pacing/retries tunable via env vars

Fixes:
- Fix dongle connection failure ("The device has no langid") when USB string
  descriptors are unavailable; dev.product/dev.manufacturer reads now wrapped
  in try/except with fallback names
- Fix UPLOAD_RESULT_STATUS_IDX from 4 to 6 to match original SiftRunner offset
- Fix upload chunk sizing to respect message payload budget (USB_MSG_LEN - 4)
- Fix USB OUT framing to use 34-byte transmit frame budget (USB_OUT_MSG_LEN)
- Fix long-upload transport stall where full RX queues blocked USB receive thread
- Fix bundle-mode asset staging to 4-byte align extracted .sftbndl records before
  CRC append
- Fix install-siftapp --install-mode auto no-fallback-on-upload-error behavior
- Fix GUI crash (EXC_BREAKPOINT) caused by calling hid.enumerate() from background
  thread on macOS; GUI bridge now uses enumerate_dongles_pyusb()
- Fix Observer tab neighbor layout: reverted to authoritative side numbering
  0=top, 1=left, 2=bottom, 3=right (confirmed from decompiled Sifteo SDK)

Tests:
- Add unit tests for .siftapp header parsing, app-ID inference, upload wiring
- Add dongle RX queue overflow, write-tuning, ACK handshake, and upload tests
- Add .sftbndl parser, bundle extraction, install-mode, and alignment tests
- Add UploadResult and CRC-learning upload tests
- Add install_siftapp tests for payload_crc_mode="learn" in bundle and opaque modes

Docs:
- Update CHANGELOG.md, README.md, TODO.md with new features and fixes

Tests:
- pytest -q tests/test_siftapp.py tests/test_protocol.py -- pending run
