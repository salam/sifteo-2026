Implement asset sync, sound pipeline, docs, and runtime upgrades

- add declarative asset support in `BaseApp`:
  - support `APP_NAME`/`APP_ID`, `IMAGES`, `SOUNDS`, computed `app_id`
  - add `asset_id()` lookup and pre-`setup()` asset sync on `run()`
  - add `sync_assets_to_cube()` for late-joining cubes
- extend asset tooling in `src/sifteo/assets.py`:
  - add manifest/sync model (`AssetManifest`, `AssetDeclaration`, `AssetSyncError`)
  - add WAV -> float32 22050Hz mono encoder with CRC (`encode_sound`)
  - add `upload_sound()` helper
  - make upload/delete response handling robust across response/event queues
- improve runtime stability:
  - add dongle write rate limiting + retry in `src/sifteo/dongle.py`
  - add short inter-cube init delay in `src/sifteo/runner.py`
- expand cube/protocol APIs:
  - add `cmd_put_pixel` and `cmd_set_rotation`
  - wire `Cube.orientation` to hardware rotation command
  - add `Cube.put_pixel*()` methods
  - request and parse firmware version events
  - parse battery low + dock state/location events into cube state
- add host-side sound playback (`src/sifteo/sound.py`) and export new APIs in
  `src/sifteo/__init__.py`
- update examples and docs:
  - extend `examples/asset_upload.py` for WAV and auto type detection
  - add `examples/hot_potato.py`
  - add `docs/asset-formats.md` and `docs/game-development-guide.md`
  - update `README.md` event examples and acknowledgements
  - mark implemented items in `TODO.md`
- add tests:
  - `tests/test_protocol.py`
  - `tests/test_cube.py`
  - `tests/__init__.py`
- add project assets/reference materials:
  - `power_up.wav`
  - `sifteo-v1.jpg`
  - `sifteo-v1-doctormikereddy/` (preserved Sifteo Sync.app bundle)
- add/maintain release tracking:
  - update `CHANGELOG.md`
  - expand `AGENTS.md` directives for changelog + next-commit-message upkeep

Tests:
- `pytest -q tests` -> `79 passed`
