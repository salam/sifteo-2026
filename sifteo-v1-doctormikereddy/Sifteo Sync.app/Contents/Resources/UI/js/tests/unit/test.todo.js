define(['chai'],
function(chai) {
  var expect = chai.expect;

  describe("TODO", function() {
    
    it('1 should equal 1', function() {
      expect(1).to.equal(1);
    });
    
  });
  
  return { name: "test.todo" }
});
